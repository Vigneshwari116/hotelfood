PGHOST=127.0.0.1
PGPORT=5434
PGDATABASE=shilpa_enterprise
PGUSER=postgres
PGPASSWORD=change-me
API_KEY=shilpa-enterprise-api
PORT=8080

# Trial expiry (UTC). Date-only means end of that day. Extend the trial by
# updating this value and restarting the API container — no code changes.
LICENSE_EXPIRES_AT=2026-10-20
